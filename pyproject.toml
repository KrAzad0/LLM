[project]
name = "deepseek"
version = "0.1.0"
description = "A minimal DeepSeek-style transformer implementation"
authors = [{name = "OpenAI Agent"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["deepseek*"]
